<h1> Yandex Dictionary</h1>

<p>
	Hello, <%= link_to ('/profile'){  current_user.username } %>. 
	<%= link_to('/logout', method: 'delete' ){ 'Logout' } %><br/>
</p>

<% if current_user && current_user.admin? %> 
  <p> 
    <%= link_to('/users/show' ){ 'Users list' } %>
  </p> 
<% end %>

<br/>
<%= form_tag('/lookup', method: "post") do %>
  <p> <%= label_tag(:lang, 'Lang: ') %>
	<% lang_list = @langs_collection.map{ |lang| [lang ,lang] } %>
	<%= select_tag(:lang, options_for_select(lang_list, 'en-ru'))%>
	<p><%= label_tag(:text, 'Text: ') %>
	<%= text_area_tag(:text) %>
	<p><%= submit_tag('Search') %>
<% end %>


<p>Last 10 requests:</p>
<% @last.reverse_each do |request| %>
	<%= request.lang %>  <%= request.text %><br/>
<% end %>

<p><%= link_to('/journal/index'){ 'Journal' } %>

<center>
	Реализовано с помощью сервиса <%= link_to('http://api.yandex.ru/dictionary/'){ '«Яндекс.Словарь»'} %>
<center>